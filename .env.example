# URL API backend — ganti sesuai IP/domain server Anda
# Akses dari host           : http://localhost:4000
# Deploy langsung ke server : http://<server-ip>:4000
# Di balik reverse proxy    : /telemarketing_qc_system/api  <- dipakai produksi
VITE_API_URL=http://localhost:4000
